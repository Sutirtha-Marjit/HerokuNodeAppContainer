export interface ToggleActionProp{
    class:string,
    el:any,
    invoker:any
}

export interface ImageObjectElement{
    image:HTMLImageElement,
    path:string,
    width:number,
    height:number,
    exists:boolean,
    status:string,
    includedInZip:boolean
}